# 安全策略

## 支持范围

项目当前只为最新 GitHub Release 提供安全修复。旧版本用户应先升级到最新版本再验证问题是否仍然存在。

## 私下报告漏洞

请不要为安全漏洞创建公开 Issue。请发送邮件至 `2601714031@qq.com`，标题以 `[DSH Desktop Security]` 开头，并尽量提供：

- 受影响版本；
- 复现步骤或最小样例；
- 潜在影响；
- 建议修复方式（如有）；
- 是否已经公开披露。

维护者会尽快确认收到报告。在修复发布前，请不要公开利用细节。

## 安全边界

- 本项目是非官方 DeepSeek Harness 桌面客户端。
- 插件和在线发现结果可能包含第三方代码，安装前应确认来源、权限和生命周期脚本。
- 主题包只允许声明式 JSON 和受限图片资源，不执行其中的脚本或 CSS。
- 应用内网页只允许当前 Harness 的本机 Origin；外部链接交给系统浏览器。
